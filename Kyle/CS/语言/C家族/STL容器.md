将运用最广泛的一些数据结构实现出来；（数组，链表，树，栈，队列，集合，映射表）
STL容器在使用前都需要引用头文件；
***
### 序列式容器
强调值的排序，每个元素都有固定的位置；
最常用的是 vector 和 list；
[[string容器]]
[[vector容器（单端数组）]]
[[deque容器（双端数组）]]
[[stack容器（栈）]]
[[queue容器（队列）]]
[[list容器（C++链表）]]

##### 数据存取
-   通过 [] 访问单个元素； （例如 str[i]）
-   通过 at() 访问单个元素； (例如 str.at(i) )
***
### 关联式容器
二叉树结构，各元素之间没有严格的顺序关系；
[[map容器（字典）]]
